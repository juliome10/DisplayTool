<%= form_for(@diapositiva) do |f| %>
  <% if @diapositiva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diapositiva.errors.count, "error") %> prohibited this diapositiva from being saved:</h2>

      <ul>
      <% @diapositiva.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
